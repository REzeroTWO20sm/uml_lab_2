1)
@startuml
start
:Вставить карту;
:Ввести пароль;
if (Пароль проверен?) then (да)
  :Проверить сумму;
  if (Сумма проверена?) then (да)
    if (Сумма достаточная?) then (да)
      :Выдать деньги;
      :Забрать карту;
    else (нет)
      :Сообщить об ошибке (недостаточная сумма);
      :Забрать карту;
    endif
  else (нет)
    :Сообщить об ошибке (ошибка проверки суммы);
    :Забрать карту;
  endif
else (нет)
  :Сообщить об ошибке (ошибка проверки пароля);
  :Забрать карту;
endif
stop
@enduml
2)
|Start| start :Введите коэффициенты a, b и c; 
:Вычислите дискриминант D = b^2 - 4ac; 
if (D > 0) then (Два корня) 
:Вычислите корни x1 и x2 по формулам; 
else if (D = 0) then (Один корень) 
:Вычислите корень x по формуле; 
else (Нет корней) 
:Выведите сообщение о том, что корней нет; 
endif |End|